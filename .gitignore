node_modules/
lib/
dist/
*.tsbuildinfo
.DS_Store

# 凭证一律不进仓库。值放 secrets/（权威副本在密码管理器），
# 仓库里只留 secrets/env.example。
secrets/*
!secrets/env.example

# 本地临时
.dshr-dev/
*.log
.claude/worktrees/
# 个人的 Claude Code 权限覆盖——**只对这台机器有意义，不进仓库**。
# dshr 是公开仓库：把「自动放行某个命令」这类策略提交上去，
# 等于替每个 clone 的人做了他们没同意过的决定。
# 项目级的共享配置写 `.claude/settings.json`（那个可以进仓库），
# 个人的放这里。
.claude/settings.local.json
